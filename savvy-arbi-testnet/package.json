{
  "name": "savvy-arbi-testnet",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "./build.sh",
    "build:subgraph": "graph build",
    "build:manifest": "ts-node --files setup/buildManifest.ts",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ savvy-arbi-testnet",
    "create-local": "graph create --node http://localhost:8020/ savvy-arbi-testnet",
    "remove-local": "graph remove --node http://localhost:8020/ savvy-arbi-testnet",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 savvy-arbi-testnet",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.48.0",
    "@graphprotocol/graph-ts": "^0.29.3",
    "chalk": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3",
    "write-yaml-file": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.2.5",
    "matchstick-as": "0.5.0"
  }
}
