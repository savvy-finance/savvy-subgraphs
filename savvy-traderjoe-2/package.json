{
  "name": "savvy-traderjoe-2",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ savvy-traderjoe-2",
    "create-local": "graph create --node http://localhost:8020/ savvy-traderjoe-2",
    "remove-local": "graph remove --node http://localhost:8020/ savvy-traderjoe-2",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 savvy-traderjoe-2",
    "test": "graph test",
    "goldsky:deploy": "graph build && goldsky subgraph deploy"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.51.2",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.2"
  }
}
